name: Release

on:
  push:
    branches: [main]

permissions:
  contents: write
  issues: write
  pull-requests: write
  id-token: write # npm trusted publishing (OIDC)

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: oven-sh/setup-bun@v2
      # No registry-url here: the .npmrc it writes conflicts with
      # semantic-release's own npm auth handling.
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      # Trusted publishing needs npm >= 11.5.1
      - run: npm install -g npm@latest
      - run: bun install --frozen-lockfile
      - run: bunx tsc --noEmit
      - run: bunx vitest run
      - run: bun run build
      - run: npx semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Bootstrap only: used for the first publish, before the npm
          # package exists and a trusted publisher can be configured.
          # Once trusted publishing is set up, delete the NPM_TOKEN secret —
          # OIDC is attempted first and this falls away naturally.
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
